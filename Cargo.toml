[workspace]
resolver = "2"
members = [
    "crates/atc-core",
    "crates/atc-cli",
]

[workspace.package]
edition = "2021"
license = "MIT"
repository = "https://github.com/harmony-labs/atc"
version = "0.1.0"

[workspace.dependencies]
# Async
async-trait = "0.1"
tokio = { version = "1.44", features = ["full"] }
futures = "0.3"

# CLI
clap = { version = "4.5", features = ["derive"] }

# Date/time
chrono = { version = "0.4.20", features = ["serde"] }

# Error handling
anyhow = "1.0"
thiserror = "2.0"

# Serialization
serde = { version = "1.0.210", features = ["derive"] }
serde_json = "1.0"
toml = "0.9"

# SQLite
sqlx = { version = "0.8", features = ["runtime-tokio", "sqlite"] }

# Temp files (for tests)
tempfile = "3"

[profile.release]
lto = "thin"
codegen-units = 16
opt-level = "s"
panic = "abort"
strip = true
